Topic: {{topic}}
Scene title: {{title}}
Learning objective: {{objective}}
Core concepts: {{coreConcepts}}
Duration: {{durationHint}} seconds

Generate animation metadata. Output JSON:
{
  "title": "string",
  "speakerNote": "string — overall narrator intro",
  "stepLabels": ["step 1 name", "step 2 name"],
  "stepDescriptions": ["step 1 explanation", "step 2 explanation"]
}
